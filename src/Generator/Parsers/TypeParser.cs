using StateSharp.Generator.Models;

namespace StateSharp.Generator.Parsers
{
    public static class TypeParser
    {
        public static TypeModel Parse()
        {
            return new TypeModel(string.Empty, string.Empty);
        }
    }
}